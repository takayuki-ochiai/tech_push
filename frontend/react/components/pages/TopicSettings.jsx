import React, { Component } from 'react';
import { withRouter } from 'react-router';

class TopicSettings extends Component {
  render() {
    return (
      <div>
        topic settings
      </div>
    );
  }
}

export default withRouter(TopicSettings);
